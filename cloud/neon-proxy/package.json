{
  "name": "gps-tico-neon-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Proxy Cloudflare Worker entre GPS TICO y Neon (guarda el secreto de la base).",
  "dependencies": {
    "@neondatabase/serverless": "^0.10.1"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  }
}
